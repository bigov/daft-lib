# Устанавливает локальную переменную "TP_LIBS", в которой хранится
# путь к каталогу внешних библиотек: SET( TP_LIBS "/d/dev/WORKSPACE/LIBS" )
include( "${CMAKE_CURRENT_LIST_DIR}/CMakeLists.txt.user.tp_libs" )

